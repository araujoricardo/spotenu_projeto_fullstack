export enum sucessMessage {

};

export enum failureMessage{
    password = "senha com menos de 6 digitos",
    role = "não existe usuario do tipo selecionado (role invalido)",
    admin = "apenas admin podem cadastrar admin"
};